# Binários e artefatos de release
/bin/
/dist/
/heimdall
*.exe

# Binários de teste e cobertura
*.test
coverage.out
coverage.txt
coverage.html
*.coverprofile

# Perfis pprof
*.prof

# Bancos locais e sidecars do SQLite (WAL/rollback)
*.db
*.db-shm
*.db-wal
*.db-journal
*.sqlite
*.sqlite3
*.sqlite-shm
*.sqlite-wal

# Artefatos de harness de agentes — estado local das ferramentas, não do produto.
# Sem âncora de propósito: também cobre cópias aninhadas (ex.:
# .maestri/roles/<id>/.commandcode/).
.maestri/
.commandcode/
