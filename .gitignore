# Binários e artefatos de release
/bin/
/dist/
/heimdall
*.exe

# Binários de teste e cobertura
*.test
coverage.out
coverage.txt
coverage.html
*.coverprofile

# Perfis pprof
*.prof

# Bancos locais e sidecars do SQLite (WAL/rollback)
*.db
*.db-shm
*.db-wal
*.db-journal
*.sqlite
*.sqlite3
*.sqlite-shm
*.sqlite-wal

# Artefatos de harness de agentes — estado local das ferramentas, não do produto.
# Sem âncora de propósito: também cobre cópias aninhadas (ex.:
# .maestri/roles/<id>/.commandcode/).
.maestri/
.commandcode/

# Dependências da SPA (F5.2b). internal/webui/dist/ É commitado de propósito:
# `go build` embute o build e precisa dele sem Node instalado. Regenere com
# `make web`.
web/node_modules/
# Catálogos gerados da SPA: derivados dos catálogos do backend + rótulos gui.*;
# regenerados por `npm run prebuild`/`pretest` (portanto por `make web`).
web/src/i18n/generated/
